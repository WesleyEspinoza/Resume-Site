import React from "react";
import "./Resume.css"

type ResumeProps = {};
type ResumeState = {};
class Button extends React.Component<ResumeProps, ResumeState> {
    constructor(props: ResumeProps, state: ResumeState) {
        super(props);
        this.state = state
    }

    render() {
        return (
            <></>
        );
    }
}

export default Button